# Recorded execution environment

The supplied project artifacts record the following principal environment:

- Python 3.11.9, 64-bit CPython
- Windows 10/11-compatible PowerShell workflow
- 4 physical / 8 logical CPU cores
- Approximately 16 GB RAM
- CPU-only execution
- Hosted generation and verification through the Groq API

The original environment report contained local absolute paths and transient resource readings; it was not copied into the public package. The dependency files and staged scripts are retained for reconstruction.
